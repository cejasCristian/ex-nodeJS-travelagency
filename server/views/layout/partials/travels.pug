.row.next-trips
            each travel in travels 
                .col-md-6.col-lg-4.mb-4 
                    .card
                        img(src=`img/destiny_${travel.image}.jpg`, class = 'img-fluid')
                        .card-body  
                            h2=travel.title
                            p   
                                i.fas.fa-calendar-alt
                                |     #{travel.departure_date} / #{travel.arrival_date}
                            p   
                                i.fas.fa-dollar-sign
                                |     #{travel.price}
                            p= travel.description.substr(0,100)
                            a(href=`travels/${travel.id}`, class='btn btn-success btn-block') More info